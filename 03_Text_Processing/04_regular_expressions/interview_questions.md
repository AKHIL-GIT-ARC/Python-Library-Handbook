# Regular Expressions — Interview Questions

## 1. What is regex?

Regex (regular expression) is a pattern used to search, match, extract, and replace text.

```python
import re
print(re.findall(r"\d+", "Age: 20"))